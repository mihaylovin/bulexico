# bulexico

A Node JS app with operations over (not) all the words in the Bulgarian language

# Usage

To instal:

```sh
npm install
```
or
```sh
yarn install
```

To start:

```sh
npm start
```
or
```sh
yarn start
```

